Let $Y_1, Y_2, ..., Y_n$ be independent random variables, each with a beta distribution, with $\alpha = \beta = 2$.
Find

a) the probability distribution function of $Y_{(n)} = \max(Y_1, Y_2, ..., Y_n)$.

b) the density function of $Y_{(n)}$.

c) $E(Y_{(n)})$ when $n = 2$.
