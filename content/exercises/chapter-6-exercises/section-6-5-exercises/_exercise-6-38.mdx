Let $Y_1$ and $Y_2$ be independent random variables with moment-generating functions $m_{Y_1}(t)$ and $m_{Y_2}(t)$, respectively.
If $a_1$ and $a_2$ are constants, and $U = a_1 Y_1 + a_2 Y_2$ show that the moment-generating function for $U$ is $m_U(t) = m_{Y_1}(a_1 t) \times m_{Y_2}(a_2 t)$.
